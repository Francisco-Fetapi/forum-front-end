import React, { useContext } from 'react'
// import AllStates from '../context/AllStates'

function Header() {
    // const {states} = useContext(AllStates);
    return (
        <div>
            
        </div>
    )
}

export default Header
